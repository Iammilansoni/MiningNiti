# =============================================================================
# MiningNiti Backend — Requirements
# AI-Powered Document Intelligence for the Mining Industry
# =============================================================================

# -----------------------------------------------------------------------------
# Core Framework
# -----------------------------------------------------------------------------
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.9.0
pydantic-settings==2.5.0

# -----------------------------------------------------------------------------
# Database & ORM
# -----------------------------------------------------------------------------
sqlalchemy==2.0.35
# psycopg3 drives both engines: sync (Alembic, migration runner, startup
# checks) and async (request handlers). See app/db/session.py.
psycopg[binary,pool]==3.2.3
psycopg2-binary==2.9.10
# SQLAlchemy's async layer runs the DBAPI inside a greenlet; it is a hard
# requirement, not an optional extra.
greenlet>=3.0.0
# Async SQLite driver, used by the test suite only.
aiosqlite==0.22.1
pgvector==0.3.0
alembic==1.13.0

# -----------------------------------------------------------------------------
# AI/ML - Google Gemini, OpenAI compat
# -----------------------------------------------------------------------------
google-generativeai==0.8.6
google-auth==2.35.0
google-api-python-client==2.140.0
openai==1.50.0
cerebras-cloud-sdk>=1.0.0

# -----------------------------------------------------------------------------
# Vector Operations
# -----------------------------------------------------------------------------
numpy==1.26.4
scikit-learn==1.5.1
sentence-transformers>=3.0.0

# -----------------------------------------------------------------------------
# Document Processing
# -----------------------------------------------------------------------------
pypdf==4.2.0
python-docx==1.1.2
python-magic==0.4.27

# Layout-aware extraction. pdfplumber recovers tables, which pypdf's flat text
# stream destroys — and mining regulations/manuals are mostly tables.
# NOTE: PyMuPDF (fitz) is deliberately NOT used here. It is more capable but
# AGPL-3.0, which is incompatible with this project's MIT licence.
pdfplumber==0.11.10

# OCR for scanned documents. Requires the tesseract-ocr and poppler-utils
# system packages (installed in the Dockerfile); the code degrades to a warning
# when they are absent so local dev without them still works.
pytesseract==0.3.13
pdf2image==1.17.0

# -----------------------------------------------------------------------------
# Background Jobs
# -----------------------------------------------------------------------------
redis==7.1.0

# -----------------------------------------------------------------------------
# HTTP & Async
# -----------------------------------------------------------------------------
httpx==0.27.2
aiofiles==24.1.0
requests==2.32.3

# -----------------------------------------------------------------------------
# Security, Auth & Rate Limiting
# -----------------------------------------------------------------------------
slowapi==0.1.9
# python-jose removed: 3.3.0 is affected by CVE-2024-33663 (algorithm
# confusion) and CVE-2024-33664 (JWE decompression DoS). JWT verification now
# uses PyJWT — see app/core/security.py.
cryptography==46.0.3
PyJWT[crypto]==2.9.0

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dateutil==2.9.0
tqdm==4.66.4

# -----------------------------------------------------------------------------
# Production Server
# -----------------------------------------------------------------------------
gunicorn==23.0.0
httptools>=0.6.3

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
black==24.8.0
isort==5.13.0
mypy==1.11.0

# -----------------------------------------------------------------------------
# MLOps — Observability
# -----------------------------------------------------------------------------
langsmith>=0.2.0
